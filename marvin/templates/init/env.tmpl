{{ $port := 	.ProjectInfo.Port -}}

# db variables
DB_HOST=localhost
DB_USER=postgres
DB_PASS=postgres
DB_NAME=test_db
DB_PORT=5432
DB_SSL=disable
DB_TZ=Europe/Helsinki

# DATABASE_URL="<db_type>://<username>:<passowrd>@127.0.0.1:5432/<db_name>?sslmode=disable"
DATABASE_URL="postgres://postgres:postgres@127.0.0.1:5432/test_db?sslmode=disable"

# go backend variables
GO_BACKEND_PORT={{$port}}
HOST_URL=http://localhost:{{$port}}
API_PATH={{$.ProjectInfo.APIPrefix}}
FRONTEND_URL=http://localhost:3000
DEBUG_MODE=true
