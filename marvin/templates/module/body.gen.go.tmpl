{{ $project  	:= .ProjectInfo -}}
{{ $module      := .Modules.Name -}}
{{ $endpoints   := .Modules.Endpoints -}}
// Code generated by gomarvin, {{$project.GomarvinVersion}}. DO NOT EDIT.
// Rename the current file and remove upper comment to save changes!

package {{ ConvertToLowercase  $module}}_module

{{/* If there is an endpoint with a body that has a time.Time field, import the time package */ -}}
{{- range $endpoint := $endpoints -}}
	{{ if $endpoint.Body -}}
			{{ range $endpoint_body_field := $endpoint.Body}}
				{{ if ( eq $endpoint_body_field.Type "time.Time") }}
import (
	"time"
)
					{{ break }}
				{{ end }}
			{{ end }}
		{{ break }}
	{{ end }}	
{{ end -}}


{{range $endpoint := $endpoints }}

	{{ if $endpoint.Body -}} 
type {{$endpoint.ControllerName}}Body struct {
		{{- range $body_field := $endpoint.Body }}
		    {{ ConvertToCamelCase $body_field.Field }}  	{{ $body_field.Type }} 	`json:"{{$body_field.Field}}" validate:"{{$body_field.Validate}}"`
	    {{- end }}
}
	{{ end -}}

{{ end }}